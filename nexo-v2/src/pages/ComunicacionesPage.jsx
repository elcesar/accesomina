import { useEffect, useMemo, useState } from 'react'
import { IconCheck, IconPlus, IconRefresh, IconSearch } from '@tabler/icons-react'
import { api } from '../services/api.js'
import { useAuth } from '../services/auth.jsx'
import '../styles/comunicaciones.css'

const rows=value=>Array.isArray(value)?value:[]
const newId=()=>globalThis.crypto?.randomUUID?.()||`callout-${Date.now()}-${Math.random().toString(16).slice(2)}`
const editors=new Set(['domian_admin','client_admin','rrhh'])
const emptyDraft=()=>({tipo:'convocatoria',titulo:'',mensaje:'',mantId:'',trabId:'',fecha:'',estado:'pendiente'})
const labelFor=item=>item.titulo||item.nombre||item.title||item.tipo||item.descripcion||'Comunicación'

export default function ComunicacionesPage(){
 const {session}=useAuth();const canEdit=editors.has(session?.user?.role)
 const [response,setResponse]=useState(null);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [selectedId,setSelectedId]=useState(null);const [creating,setCreating]=useState(false);const [draft,setDraft]=useState(emptyDraft);const [search,setSearch]=useState('');const [statusFilter,setStatusFilter]=useState('');const [message,setMessage]=useState('');const [messageTone,setMessageTone]=useState('')
 async function load(){setLoading(true);setMessage('');try{setResponse(await api.get('/state'))}catch{setMessage('No fue posible cargar las comunicaciones.');setMessageTone('error')}finally{setLoading(false)}}
 useEffect(()=>{load()},[])
 const state=response?.state||response||{};const callouts=useMemo(()=>rows(state.callouts),[state.callouts]);const orders=useMemo(()=>rows(state.mantenciones),[state.mantenciones]);const workers=useMemo(()=>rows(state.trabajadores),[state.trabajadores]);const orderById=useMemo(()=>new Map(orders.map(x=>[String(x.id),x])),[orders]);const workerById=useMemo(()=>new Map(workers.map(x=>[String(x.id),x])),[workers])
 const visible=useMemo(()=>{const term=search.trim().toLowerCase();return callouts.filter(item=>{if(statusFilter&&String(item.estado||item.status||'pendiente')!==statusFilter)return false;if(!term)return true;const order=orderById.get(String(item.mantId));const worker=workerById.get(String(item.trabId||item.trabajadorId||item.personaId));return[labelFor(item),item.mensaje,item.descripcion,order?.nombre,worker?.nombre].filter(Boolean).some(v=>String(v).toLowerCase().includes(term))})},[callouts,search,statusFilter,orderById,workerById])
 const selected=callouts.find(x=>String(x.id)===String(selectedId))||null;const pendingCount=callouts.filter(x=>!['confirmado','completado','cerrado','cancelado'].includes(String(x.estado||x.status||'pendiente').toLowerCase())).length;const confirmedCount=callouts.filter(x=>['confirmado','completado'].includes(String(x.estado||x.status||'').toLowerCase())).length
 useEffect(()=>{if(!callouts.length){setSelectedId(null);if(!creating)setDraft(emptyDraft());return}const current=callouts.find(x=>String(x.id)===String(selectedId))||callouts[0];if(String(current.id)!==String(selectedId))setSelectedId(current.id);if(!creating)setDraft({...emptyDraft(),...current,titulo:current.titulo||current.nombre||current.title||'',mensaje:current.mensaje||current.descripcion||''})},[callouts,selectedId,creating])
 const update=key=>event=>setDraft(current=>({...current,[key]:event.target.value}))
 const selectItem=id=>{setCreating(false);setSelectedId(id);setMessage('');setMessageTone('')}
 async function save(event){event.preventDefault();if(!canEdit)return;if(draft.titulo.trim().length<2){setMessage('Indica un título para la comunicación.');setMessageTone('error');return}if(draft.trabId&&!workers.some(x=>String(x.id)===String(draft.trabId))){setMessage('La persona seleccionada ya no está disponible.');setMessageTone('error');return}if(draft.mantId&&!orders.some(x=>String(x.id)===String(draft.mantId))){setMessage('La orden seleccionada ya no está disponible.');setMessageTone('error');return}setSaving(true);setMessage('');try{const id=creating?(draft.id||newId()):selectedId;const now=new Date().toISOString();const saved={...draft,id,titulo:draft.titulo.trim(),nombre:draft.titulo.trim(),mensaje:draft.mensaje.trim(),descripcion:draft.mensaje.trim(),updatedAt:now,...(creating?{createdAt:draft.createdAt||now}:{})};const next=creating?[...callouts,saved]:callouts.map(x=>String(x.id)===String(id)?saved:x);const result=await api.put('/state/modules',{changes:{callouts:{version:Number(response?.moduleVersions?.callouts||0),data:next}},reason:creating?'Comunicación o convocatoria creada':'Comunicación o convocatoria actualizada'});setResponse(current=>({...(current||{}),state:{...(current?.state||current||{}),callouts:next},moduleVersions:{...(current?.moduleVersions||{}),...result.moduleVersions}}));setSelectedId(id);setCreating(false);setDraft(saved);setMessage(creating?'Comunicación creada.':'Cambios guardados con trazabilidad.');setMessageTone('ok')}catch(error){setMessage(error.message||'No fue posible guardar la comunicación.');setMessageTone('error')}finally{setSaving(false)}}
 return <section className="nk-comms-page"><header className="nk-comms-header"><div><p className="nk-comms-kicker">Gestión operacional</p><h1>Comunicaciones y convocatorias</h1><p>Coordina convocatorias, mensajes y confirmaciones vinculadas a personas y órdenes de servicio.</p></div><div className="nk-actions"><button className="nk-button nk-button-secondary" type="button" onClick={load} disabled={loading}><IconRefresh size={16}/>Actualizar</button>{canEdit&&<button className="nk-button nk-button-primary" type="button" onClick={()=>{setCreating(true);setSelectedId(null);setDraft({...emptyDraft(),id:newId()});setMessage('')}}><IconPlus size={16}/>Nueva comunicación</button>}</div></header>{message&&<div className={`nk-comms-feedback ${messageTone}`}>{message}</div>}<section className="nk-comms-summary"><article><b>{callouts.length}</b><span>Comunicaciones</span></article><article><b>{pendingCount}</b><span>Pendientes</span></article><article><b>{confirmedCount}</b><span>Confirmadas</span></article></section><div className="nk-comms-layout"><aside className="nk-comms-list nk-card"><div className="nk-comms-list-head"><strong>Registro</strong><span className="nk-badge nk-badge-none">{visible.length}/{callouts.length}</span></div><div className="nk-comms-filters"><label className="nk-search"><IconSearch size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar..." aria-label="Buscar comunicaciones"/></label><select className="nk-select" value={statusFilter} onChange={e=>setStatusFilter(e.target.value)} aria-label="Filtrar por estado"><option value="">Todos</option><option value="pendiente">Pendiente</option><option value="enviado">Enviado</option><option value="confirmado">Confirmado</option><option value="cancelado">Cancelado</option></select></div>{loading?<div className="nk-comms-empty">Cargando…</div>:!visible.length?<div className="nk-comms-empty">No hay comunicaciones para mostrar.</div>:visible.map(item=><button type="button" key={item.id} className={`nk-comms-list-item ${String(item.id)===String(selectedId)&&!creating?'active':''}`} onClick={()=>selectItem(item.id)}><strong>{labelFor(item)}</strong><small>{orderById.get(String(item.mantId))?.nombre||'Sin orden'} · {workerById.get(String(item.trabId||item.trabajadorId||item.personaId))?.nombre||'Sin persona'}</small><small>{item.estado||item.status||'pendiente'}</small></button>)}</aside><form className="nk-comms-workspace nk-card" onSubmit={save}><div className="nk-comms-workspace-head"><div><p className="nk-comms-kicker">{creating?'Nueva comunicación':'Detalle'}</p><h2>{creating?'Registrar comunicación':draft.titulo||'Comunicación'}</h2></div>{canEdit&&<button className="nk-button nk-button-primary" type="submit" disabled={saving}><IconCheck size={16}/>{saving?'Guardando…':'Guardar cambios'}</button>}</div><div className="nk-comms-form-grid"><label className="nk-field"><span className="nk-label">Tipo</span><select className="nk-select" disabled={!canEdit} value={draft.tipo} onChange={update('tipo')}><option value="convocatoria">Convocatoria</option><option value="comunicacion">Comunicación</option><option value="recordatorio">Recordatorio</option></select></label><label className="nk-field"><span className="nk-label">Estado</span><select className="nk-select" disabled={!canEdit} value={draft.estado} onChange={update('estado')}><option value="pendiente">Pendiente</option><option value="enviado">Enviado</option><option value="confirmado">Confirmado</option><option value="cancelado">Cancelado</option></select></label><label className="nk-field nk-comms-wide"><span className="nk-label">Título</span><input className="nk-input" disabled={!canEdit} required value={draft.titulo} onChange={update('titulo')} placeholder="Convocatoria a turno"/></label><label className="nk-field"><span className="nk-label">Orden de servicio</span><select className="nk-select" disabled={!canEdit} value={draft.mantId} onChange={update('mantId')}><option value="">Sin orden asociada</option>{orders.map(x=><option key={x.id} value={x.id}>{x.codigo?`${x.codigo} · `:''}{x.nombre}</option>)}</select></label><label className="nk-field"><span className="nk-label">Persona</span><select className="nk-select" disabled={!canEdit} value={draft.trabId} onChange={update('trabId')}><option value="">Sin persona específica</option>{workers.map(x=><option key={x.id} value={x.id}>{x.nombre}</option>)}</select></label><label className="nk-field"><span className="nk-label">Fecha</span><input className="nk-input" type="date" disabled={!canEdit} value={draft.fecha} onChange={update('fecha')}/></label><label className="nk-field nk-comms-wide"><span className="nk-label">Mensaje / detalle</span><textarea className="nk-textarea" rows="5" disabled={!canEdit} value={draft.mensaje} onChange={update('mensaje')} placeholder="Información que recibirá o deberá confirmar la persona."/></label></div>{!creating&&selected&&<section className="nk-comms-context"><strong>Contexto operacional</strong><span>Orden: {orderById.get(String(selected.mantId))?.nombre||'Sin orden asociada'}</span><span>Persona: {workerById.get(String(selected.trabId||selected.trabajadorId||selected.personaId))?.nombre||'Sin persona específica'}</span></section>}</form></div></section>
}